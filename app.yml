# Forward-looking GitHub App manifest. This release ships as a public
# containerized Action; a hosted App identity is not required for install.
name: dsh-migrate
url: https://github.com/royenheart/dsh-migrate-bot
default_permissions:
  contents: write
  issues: write
  pull_requests: write
  metadata: read
default_events: []
