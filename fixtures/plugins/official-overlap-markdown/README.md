# fixture: official-overlap-markdown

Shadows the official `conversation.chat.node` / `assistant-step` renderer
(the same cell-shadowing trick as a full-markdown plugin). Official
`MarkdownText` already covers GFM and KaTeX; this fixture exists so review
step A can flag overlap.
